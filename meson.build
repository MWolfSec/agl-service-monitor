project('agl-service-monitor',
        'cpp',
        license : 'Apache-2.0',
        default_options : ['c_std=c17', 'cpp_std=c++17'])

systemd_dep = dependency('systemd')

subdir('src')
subdir('systemd')

